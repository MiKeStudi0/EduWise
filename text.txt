seo_metadata
- id
- meta_title
- meta_description
- keywords              -- JSON / TEXT[]
- canonical_url
- robots

-- Open Graph
- og_title
- og_description
- og_type
- og_url
- og_site_name
- og_image_url
- og_image_width
- og_image_height
- og_image_alt

-- Twitter
- twitter_card
- twitter_title
- twitter_description
- twitter_image

- created_at
- updated_at


roadmaps
- id
- slug
- slug_icon
- title
- description
- seo_id          -- FK → seo_metadata.id
- order_index
- is_active
- created_at
- updated_at


technologies
- id
- roadmap_id
- slug
- slug_icon
- title
- description
- seo_id          -- FK → seo_metadata.id
- order_index
- is_active
- created_at
- updated_at

modules
- id
- roadmap_id
- technology_id
- slug
- title
- description
- seo_id          -- FK → seo_metadata.id
- is_active
- order_index
- created_at
- updated_at

topics
- id
- roadmap_id
- technology_id
- module_id
- slug
- title
- description
- examples :{
    - title
    - description
    - code_snippet
    - output
    - images
    - video_url
}       
- image_banner_url
- images             -- JSON / TEXT[]
- video_url
- when_to_use         -- JSON / TEXT[]
- when_to_avoid       -- JSON / TEXT[]
- problems -[what problem does this topic solve?] -- JSON / TEXT[]
- mental_models -[what the topic related to real world Example?] -- JSON / TEXT[]
- common_mistakes -[what are the common mistakes when learning this topic?] -- JSON / TEXT[]
- bonus_tips -[any bonus tips related to this topic?] -- JSON / TEXT[]
- related_topics -[what are the related topics?] -- JSON / TEXT[]
- seo_id          -- FK → seo_metadata.id
- is_active
- order_index
- created_at
- updated_at


sub_topics
- id
- roadmap_id
- technology_id
- module_id
- topic_id
- slug
- title
- description
- examples          -- JSON / TEXT[title,description,code_snippet,output,images,video_url]
- image_banner_url
- images             -- JSON / TEXT[]
- video_url
- when_to_use         -- JSON / TEXT[]
- when_to_avoid       -- JSON / TEXT[]
- problems -[what problem does this topic solve?] -- JSON / TEXT[]
- mental_models -[what the topic related to real world Example?] -- JSON / TEXT[]
- common_mistakes -[what are the common mistakes when learning this topic?] -- JSON / TEXT[]
- bonus_tips -[any bonus tips related to this topic?] -- JSON / TEXT[]
- related_topics -[what are the related topics?] -- JSON / TEXT[]
- seo_id          -- FK → seo_metadata.id
- is_active
- order_index
- created_at
- updated_at


lessons
- id
- roadmap_id
- technology_id
- module_id
- topic_id
- slug
- title
- description
- examples          -- JSON / TEXT[title,description,code_snippet,output,images,video_url]
- image_banner_url
- images             -- JSON / TEXT[]
- video_url
- when_to_use         -- JSON / TEXT[]
- when_to_avoid       -- JSON / TEXT[]
- problems -[what problem does this topic solve?] -- JSON / TEXT[]
- mental_models -[what the topic related to real world Example?] -- JSON / TEXT[]
- common_mistakes -[what are the common mistakes when learning this topic?] -- JSON / TEXT[]
- bonus_tips -[any bonus tips related to this topic?] -- JSON / TEXT[]
- related_topics -[what are the related topics?] -- JSON / TEXT[]
- seo_id          -- FK → seo_metadata.id
- is_active
- order_index
- created_at
- updated_at

